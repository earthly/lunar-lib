VERSION 0.8

test:
    FROM alpine:3.21
    WORKDIR /work
    RUN apk add --no-cache jq git grep sed coreutils findutils
    COPY --dir scripts test .
    RUN chmod +x scripts/check-pr-title.sh test/run_tests.sh
    RUN ./test/run_tests.sh
