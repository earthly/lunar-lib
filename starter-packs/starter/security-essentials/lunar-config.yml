version: 0

# ============================================================================
# Starter Pack: Security Essentials
# ============================================================================

collectors:
  # --- Language collectors (only trigger when language is detected) ---
  - uses: ./collectors/golang
  - uses: ./collectors/java
  - uses: ./collectors/nodejs
  - uses: ./collectors/python
  - uses: ./collectors/rust
  - uses: ./collectors/php
  - uses: ./collectors/cpp
  - uses: ./collectors/dotnet
  - uses: ./collectors/html
  - uses: ./collectors/shell
  - uses: ./collectors/ruby

  # --- Security scanners ---
  - uses: ./collectors/gitleaks
  - uses: ./collectors/trivy
  - uses: ./collectors/syft
  - uses: ./collectors/semgrep

  # --- Infrastructure context ---
  - uses: ./collectors/docker
  - uses: ./collectors/github
  - uses: ./collectors/github-actions

policies:
  # --- Secret detection ---
  - uses: ./policies/secrets
    enforcement: block-pr
    include:
      - no-hardcoded-secrets

  - uses: ./policies/secrets
    enforcement: score
    include:
      - executed

  # --- SCA ---
  - uses: ./policies/sca
    enforcement: score
    include:
      - executed
      - max-severity
    with:
      min_severity: "critical"

  # --- SAST ---
  - uses: ./policies/sast
    enforcement: score
    include:
      - executed
      - max-severity
    with:
      min_severity: "high"

  # --- Container scanning ---
  - uses: ./policies/container-scan
    enforcement: score
    include:
      - executed
      - max-severity
    with:
      min_severity: "critical"

  # --- GitHub Actions security ---
  - uses: ./policies/github-actions
    enforcement: score
    include:
      - no-script-injection
      - no-dangerous-trigger-checkout
      - permissions-declared
      - no-write-all-permissions
      - checkout-no-persist-credentials
      - no-secrets-inherit

  # --- Dockerfile ---
  - uses: ./policies/container
    enforcement: score
    include:
      - no-latest
      - user
      - healthcheck
      - stable-tags
      - dockerfile-lint-clean

  # --- SBOM ---
  - uses: ./policies/sbom
    enforcement: score
    include:
      - sbom-exists
      - has-licenses

  # --- Branch protection ---
  - uses: ./policies/vcs
    enforcement: score
    include:
      - branch-protection-enabled
      - require-pull-request
      - minimum-approvals
      - require-codeowner-review
      - disallow-force-push
