version: 0

# ============================================================================
# Starter Pack: Code Quality & Standards
# ============================================================================

collectors:
  # --- Language collectors (only trigger when language is detected) ---
  - uses: github://earthly/lunar-lib/collectors/golang@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/java@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/nodejs@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/python@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/rust@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/php@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/cpp@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/dotnet@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/html@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/shell@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/ruby@v1.0.5

  # --- Repo health ---
  - uses: github://earthly/lunar-lib/collectors/readme@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/codeowners@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/repo-boilerplate@v1.0.5
  - uses: github://earthly/lunar-lib/collectors/github@v1.0.5

  # --- Quality tools ---
  - uses: github://earthly/lunar-lib/collectors/codecov@v1.0.5

policies:
  # --- Repo standards ---
  - uses: github://earthly/lunar-lib/policies/repo-boilerplate@v1.0.5
    enforcement: score
    include:
      - readme-exists
      - codeowners-exists
      - gitignore-exists
      - license-exists
      - readme-min-line-count
      - editorconfig-exists
      - security-exists
      - contributing-exists

  # --- Code ownership ---
  - uses: github://earthly/lunar-lib/policies/codeowners@v1.0.5
    enforcement: score
    include:
      - exists
      - valid
      - catchall
      - min-owners

  # --- Testing ---
  - uses: github://earthly/lunar-lib/policies/testing@v1.0.5
    enforcement: score
    include:
      - executed
      - passing
      - coverage-collected
      - coverage-reported

  # --- Linting ---
  - uses: github://earthly/lunar-lib/policies/linter@v1.0.5
    enforcement: score
    include:
      - ran

  # --- Dependencies ---
  - uses: github://earthly/lunar-lib/policies/dependencies@v1.0.5
    enforcement: score
    include:
      - min-versions

  # --- VCS ---
  - uses: github://earthly/lunar-lib/policies/vcs@v1.0.5
    enforcement: score
    include:
      - branch-protection-enabled
      - require-pull-request

  # --- Language-specific guardrails ---
  # Version checks use sensible defaults. Override with `with:` to match your stack:
  #   min_go_version: "1.21"  |  min_java_version: "17"
  #   min_node_version: "18"  |  min_python_version: "3.9"

  # Go
  - uses: github://earthly/lunar-lib/policies/golang@v1.0.5
    enforcement: score
    include:
      - go-mod-exists
      - go-sum-exists
      - min-go-version
      - tests-recursive
    # with:
    #   min_go_version: "1.22"

  # Java
  - uses: github://earthly/lunar-lib/policies/java@v1.0.5
    enforcement: score
    include:
      - build-tool-wrapper-exists
      - min-java-version
      - tests-all-modules
    # with:
    #   min_java_version: "21"

  # Node.js
  - uses: github://earthly/lunar-lib/policies/nodejs@v1.0.5
    enforcement: score
    include:
      - lockfile-exists
      - typescript-configured
      - engines-pinned
      - min-node-version
    # with:
    #   min_node_version: "20"

  # Python
  - uses: github://earthly/lunar-lib/policies/python@v1.0.5
    enforcement: score
    include:
      - lockfile-exists
      - linter-configured
      - min-python-version

  # Rust
  - uses: github://earthly/lunar-lib/policies/rust@v1.0.5
    enforcement: score
    include:
      - cargo-toml-exists
      - cargo-lock-exists
      - min-rust-edition
      - clippy-clean

  # PHP
  - uses: github://earthly/lunar-lib/policies/php@v1.0.5
    enforcement: score
    include:
      - composer-json-exists
      - composer-lock-exists
      - phpunit-configured
      - static-analysis-configured
      - min-version

  # C/C++
  - uses: github://earthly/lunar-lib/policies/cpp@v1.0.5
    enforcement: score
    include:
      - build-system-exists
      - min-cpp-standard
      - cppcheck-clean

  # .NET
  - uses: github://earthly/lunar-lib/policies/dotnet@v1.0.5
    enforcement: score
    include:
      - project-file-exists
      - target-framework-set
      - dependencies-locked
      - test-project-exists

  # Shell
  - uses: github://earthly/lunar-lib/policies/shell@v1.0.5
    enforcement: score
    include:
      - shellcheck-clean

  # Ruby
  - uses: github://earthly/lunar-lib/policies/ruby@v1.0.5
    enforcement: score
    include:
      - gemfile-exists
      - lockfile-exists
      - ruby-version-set
      - bundler-audit-clean

  # HTML/CSS
  - uses: github://earthly/lunar-lib/policies/html@v1.0.5
    enforcement: score
    include:
      - htmlhint-clean
      - stylelint-clean
