version: 0

name: html
description: Detect HTML and CSS/preprocessor files for frontend markup categorization
author: earthly

default_image: earthly/lunar-lib:base-main

landing_page:
  display_name: "HTML Collector"
  long_description: |
    Detect HTML, CSS, SCSS, and LESS files to categorize frontend markup projects.
    Distinguishes primarily-frontend repos from repos with incidental HTML.
    Lightweight detection collector — no external tools required.
  categories: ["languages"]
  icon: "assets/html.svg"
  status: "beta"

collectors:
  - name: project
    description: |
      Scans the repository for HTML and CSS-family files (.html, .css, .scss,
      .less). Counts files of each type and writes a separate .lang.<type> entry
      per detected type. Skips gracefully if no matching files are found.
    mainBash: project.sh
    hook:
      type: code
    keywords: ["html", "css", "scss", "less", "frontend", "web", "markup", "stylesheet"]

example_component_json: |
  {
    "lang": {
      "html": {
        "file_count": 12,
        "source": { "tool": "html", "integration": "code" }
      },
      "css": {
        "file_count": 8,
        "source": { "tool": "html", "integration": "code" }
      },
      "scss": {
        "file_count": 3,
        "source": { "tool": "html", "integration": "code" }
      },
      "less": {
        "file_count": 2,
        "source": { "tool": "html", "integration": "code" }
      }
    }
  }
